import { Component } from '@angular/core';

/**
 * MissionHeaderComponent
 *
 * Type: Presentational
 *
 * Scope:
 * - Responsible for rendering the header of the mission.
 * - Manages the styling and layout necessary for the header presentation.
 *
 * Out-of-Scope:
 * - Should not handle data fetching.
 * - Contains no business logic related to the item.
 * - Does not manage or transform the item's data.
 *
 * Purpose (optional):
 * To visually present an individual header item.
 */
@Component({
    selector: 'app-mission-header',
    imports: [],
    templateUrl: './mission-header.component.html',
})
export class MissionHeaderComponent {}
